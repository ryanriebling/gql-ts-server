import { SchemaLoader, SingleFileOptions } from '@graphql-toolkit/common';
import { ClientConfig } from 'apollo-language-server';
export declare class ApolloEngineLoader implements SchemaLoader {
    loaderId(): string;
    canLoad(ptr: string): Promise<boolean>;
    load(_: 'apollo-engine', options: ClientConfig & SingleFileOptions): Promise<{
        location: string;
        schema: import("graphql").GraphQLSchema;
    }>;
}
