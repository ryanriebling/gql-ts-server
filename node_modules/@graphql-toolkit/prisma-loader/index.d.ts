import { UrlLoader, LoadFromUrlOptions } from '@graphql-toolkit/url-loader';
interface PrismaLoaderOptions extends LoadFromUrlOptions {
    envVars?: {
        [key: string]: string;
    };
    graceful?: boolean;
}
export declare class PrismaLoader extends UrlLoader {
    loaderId(): string;
    canLoad(prismaConfigFilePath: string): Promise<boolean>;
    load(prismaConfigFilePath: string, options: PrismaLoaderOptions): Promise<import("@graphql-toolkit/common").Source>;
}
export {};
