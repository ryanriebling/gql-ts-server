import { UniversalLoader, SingleFileOptions } from '@graphql-toolkit/common';
export declare class GitLoader implements UniversalLoader {
    loaderId(): string;
    canLoad(pointer: string): Promise<boolean>;
    load(pointer: string, options: SingleFileOptions): Promise<import("@graphql-toolkit/common").Source | {
        location: string;
        document: import("graphql").DocumentNode;
        rawSDL: string;
    }>;
}
